<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/fragmentHome">

    <fragment
        android:id="@+id/fragmentHome"
        android:name="com.example.equipouno.view.fragments.HomeFragment"
        android:label="HomeFragment"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_homeFragment_to_instructionsFragment"
            app:destination="@id/fragmentInstructions" />
        <action
            android:id="@+id/action_homeFragment_to_challengesFragment"
            app:destination="@id/fragmentChallenges" />
    </fragment>

    <fragment
        android:id="@+id/fragmentInstructions"
        android:name="com.example.equipouno.view.fragments.InstructionsFragment"
        android:label="InstructionsFragment"
        tools:layout="@layout/fragment_instructions">
        <!-- Es posible que esta accion se haga através de un popBackStack() -->
        <!--
        <action
            android:id="@+id/action_instructionsFragment_to_homeFragment"
            app:destination="@id/fragmentHome" />
        -->
    </fragment>

    <fragment
        android:id="@+id/fragmentChallenges"
        android:name="com.example.equipouno.view.fragments.ChallengesFragment"
        android:label="ChallengesFragment"
        tools:layout="@layout/fragment_challenges">
        <!-- Es posible que esta accion se haga através de un popBackStack() -->
        <!--
        <action
            android:id="@+id/action_challengesFragment_to_homeFragment"
            app:destination="@id/fragmentHome" />
        -->
    </fragment>

</navigation>